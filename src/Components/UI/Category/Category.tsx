import React, { FC, JSX } from "react"
import style from "./style.module.css"

const Category:FC = ({category}):JSX.Element => {
    
    return (
        <div className={}>{category}</div>
    )
}

export default Category